---
title: Commands
description: Overview of BrowserShell's 86 built-in commands.
weight: 70
group: Reference
---

BrowserShell ships **86 commands** organized by category. Every command includes `help` text, a `man` page, tab completion, and examples.

## Quick reference by category

| Category | Examples |
|----------|----------|
| **Navigation** | `go`, `open`, `back`, `forward`, `reload`, `close`, `search` |
| **Tabs** | `tabs`, `tab`, `pin`, `unpin`, `discard`, `domain`, `title` |
| **Windows** | `windows`, `window`, `recent`, `sessions`, `mute`, `find` |
| **Bookmarks** | `bookmarks`, `bookmark` |
| **History** | `history` |
| **Downloads** | `downloads` |
| **Page** | `links`, `click`, `fill`, `read`, `shot`, `inputs`, `meta`, `tech` |
| **Privacy** | `forget`, `siteinfo`, `permissions` |
| **Dev** | `cookies`, `reqs`, `frames`, `audit`, `perf`, `env` |
| **AI** | `ai summarize`, `ai explain` |
| **Builtin** | `ls`, `cd`, `cat`, `grep`, `alias`, `export`, `clear` |
| **Utility** | `config`, `watch`, `notify`, `overlay`, `clip`, `session` |

## Discover commands

```bash
help                  # categorized list
apropos tab            # search by keyword
search bookmark        # fuzzy suggestions
man downloads          # full manual
```

## Full reference

The complete auto-generated reference (synced from the command registry):

**[Command reference →](/docs/commands/reference/)**